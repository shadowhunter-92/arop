services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: arop
      POSTGRES_USER: arop
      POSTGRES_PASSWORD: arop_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arop"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
