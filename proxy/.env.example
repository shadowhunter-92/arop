DATABASE_URL=postgresql+psycopg://arop:arop_dev@localhost:5432/arop
AROP_MASTER_KEY=changeme-generate-with-openssl-rand-hex-32
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Privacy settings (defaults shown)
HASH_PAYLOADS=true    # store SHA-256 hashes only, no plaintext in DB
STORE_RAW=false       # set true to also store raw request/response bodies
RAW_STORAGE_URL=      # optional: s3://bucket/prefix for raw payload storage
